The project is broken up into separate parts:

1. A non-preemptive round-robin scheduler-based RTOS 
2. A pre-emptive round-robin scheduler-based RTOS. Preemption comes from the scheduler, not an external interrupt. 
3. IEEE format report
